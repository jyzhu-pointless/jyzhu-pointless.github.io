source 'https://rubygems.org'

# Use the GitHub Pages gem so the local environment matches GitHub Actions / Pages build
gem 'github-pages', group: :jekyll_plugins

# If you prefer a lighter setup, replace the above with:
# gem 'jekyll', '~> 4.2'
# gem 'jekyll-remote-theme'
